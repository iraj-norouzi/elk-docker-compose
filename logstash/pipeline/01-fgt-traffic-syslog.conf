input {
  udp {
    port => 5140
    type => "syslog"
  }
}
filter {
    mutate {
      gsub => [
          "message", "^<[0-9][0-9][0-9]>", ""]
    }
    kv { }
}
output {
  elasticsearch {
    hosts => [ "elasticsearch:9200" ]
    index => "fortigate-%{+YYYY.MM.dd}"
  }
}
